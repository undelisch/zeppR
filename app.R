library(shiny)
library(bslib)
library(DT)
library(ggplot2)

source("ui/ui.R")
source("server/server.R")

shinyApp(ui, server)

# Written by undelisch // github.com/undelisch
# This software is free, and open source 
# The only thing I ask you to do if you want to use this is to pet a cat. 